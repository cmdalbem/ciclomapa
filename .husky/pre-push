#!/usr/bin/env sh
# Format, lint, typecheck, and tests run on push/PR in GitHub Actions (.github/workflows/ci.yml).
# Pre-push stays fast so local WIP does not block pushes; fix failures in CI or run yarn locally before opening a PR.
exit 0
